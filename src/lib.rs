use crate::tree::leaf;
use std::io;

pub mod tree;

pub fn run() -> () {
    println!("How are you doing?");
    let how_are_you = String::new();
    let no_int = 'b';
    //io::stdin().read_line(&mut how_are_you).except("Failed to load");


}
